name: MANIAC build & tests

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code (with submodules)
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            gfortran \
            make \
            makedepf90

      - name: Run build.sh
        run: ./build.sh

      # Install a pre-built LAMMPS binary usable for your writer tests
      - name: Install LAMMPS
        run: sudo apt-get update && sudo apt-get install -y lammps

      # Run writer tests
      - name: Run writer tests
        working-directory: tests/writers
        env:
          LMP: lmp
        run: ./run-test.sh

      # Run all reader tests
      - name: Run all reader tests
        run: |
          for testdir in tests/readers/*; do
            echo "Running reader test in $testdir"
            (cd "$testdir" && ./run-test.sh)
          done


      # Run integration tests
      - name: Run all integration tests
        run: |
          for testdir in tests/integration/energy/*; do
            echo "Running test in $testdir"
            (cd "$testdir" && ./run-test.sh)
          done

      # Run unit tests
      - name: Run all unit tests
        working-directory: tests/units
        run: ./run-test.sh

      # Run CLI tests
      - name: Run CLI tests
        working-directory: tests/cli
        run: ./run-test.sh